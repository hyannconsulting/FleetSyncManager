@inherits LayoutComponentBase

<div class="page">
    <div class="sidebar">
        <NavMenu />
    </div>

    <main>
        <div class="top-row px-4 auth">
            <div class="d-flex align-items-center">
                <h6 class="mb-0 text-muted me-3">FleetSyncManager</h6>
                <div class="ms-auto">
                    <AuthorizeView>
                        <Authorized>
                            <span class="text-light me-3">Bienvenue @context.User.Identity?.Name</span>
                            <a href="/Account/Logout" class="btn btn-outline-light btn-sm">Se déconnecter</a>
                        </Authorized>
                        <NotAuthorized>
                            <a href="/Account/Login" class="btn btn-outline-light btn-sm me-2">Se connecter</a>
                            <a href="/Account/Register" class="btn btn-light btn-sm">S'inscrire</a>
                        </NotAuthorized>
                    </AuthorizeView>
                </div>
            </div>
        </div>

        <article class="content px-4">
            @Body
        </article>
    </main>
</div>

<div id="blazor-error-ui">
    Une erreur s'est produite. Cette application peut ne plus répondre jusqu'à ce qu'elle soit rechargée.
    <a href="" class="reload">⟳ Recharger</a>
    <a class="dismiss">🗙</a>
</div>
